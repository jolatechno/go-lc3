# Golang CircleCI 2.0 configuration file
version: 2
jobs:
  build:
    docker:
      - image: circleci/golang:1.9

    working_directory: /go/src/github.com/jolatechno/go-lc3
    steps:
      - checkout
      - run: go get -v -t -d ./src/...
      - run: go test -v ./src/...
